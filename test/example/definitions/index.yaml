schemas:
  Pet:
    $ref: ./Pet.yaml
  Pets:
    type: array
    items:
      $ref: "#/components/schemas/Pet"
  Error:
    $ref: Error.yaml
  RemoteError:
    $ref: https://raw.githubusercontent.com/superbogy/zly/master/test/example/definitions/Error.yaml
